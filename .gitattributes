# Normalize line endings: LF in the repo across all platforms
* text=auto eol=lf

# Windows batch files need CRLF
*.bat text eol=crlf

# Binary assets (none yet, but future-proof)
*.png binary
*.jpg binary
*.ico binary
